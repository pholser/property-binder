Please see http://pholser.github.com/property-binder for information.

TODO:
* java.util.regex.Pattern (+ array/list/opt) tests
* Java 8 date/time artifacts (+ array/list/opt) and tests
* java.lang.Class (+ array/list/opt) and tests
  * Class<? extends T> where T is a java.lang.Class (no other j.l.r.Type)
  * raw Class
* Modularize
* Chained conversions (e.g. Path <- URI <- String)
* Allow conversions to generic types, if methods allowed
  * e.g. Set<PosixFilePermissions> from PosixFilePermissions.fromString(s)
  * TypeTokens?

* Write a small program more or less duplicating a test
  * put PB jar on module path
  * put PB jar on class path
  * how do these differ?
